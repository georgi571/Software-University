function projectCreation(name, numberOfProjects) {
    let timeNeededForOneProject = 3;
    let totalTimeToFinishAllProjects = numberOfProjects * timeNeededForOneProject;

    console.log(`The architect ${name} will need ${totalTimeToFinishAllProjects} hours to complete ${numberOfProjects} project/s.`);
}